# CLI for TiNT

Command Line Interface for TiNT.

## Usage

```
Usage: todo [hh:mm] [hh:mm] title
            [hh:mm] . title
            [hh:mm] title
            -h --help
            -v --version
            -l --list
            -g --get
            -d --delete id
            -c --clear
```
